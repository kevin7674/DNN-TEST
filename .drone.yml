workspace:
  base: /go
  path: src/github.com/kevin7674/DNN-TEST

pipeline:
  backend:
    image: golang
    commands:
      - go get
      - go build test.go
      - go test test.go     
      
  #deps:
    #image: golang:latest
    #commands:
      #- go get
      #- go test
  #build:
    #image: node:latest
    #commands:
      #- go build      

+branches: master

